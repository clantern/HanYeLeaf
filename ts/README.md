# TypeScript Client

This folder contains the TypeScript implementation of the portable HanYe Leaf core.

It intentionally mirrors the JSON-safe state shape used by the project’s simulation and analysis tooling, while remaining suitable for CLI apps and future site work.

- `ts/core`: the portable core logic and JSON-serializable state
- `ts/cli`: command-line entrypoints for examples or local experimentation
